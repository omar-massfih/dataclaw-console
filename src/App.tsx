export default function App() {
  return (
    <main className="app-shell">
      <section className="card" aria-labelledby="app-title">
        <p className="eyebrow">DataClaw Console</p>
        <h1 id="app-title">Frontend scaffold is ready</h1>
        <p className="muted">
          Start building features in <code>src/features</code> and keep shared utilities in{' '}
          <code>src/lib</code>.
        </p>
      </section>
    </main>
  );
}
